apply plugin: "java"

repositories {
    mavenLocal()
    maven {
        url 'https://jitpack.io'
    }
}

dependencies {
    compile 'com.github.jvm-redux:jvm-redux-api:1.2.0'
    compile project(":redux-javafx-devtool")
}