apply plugin: "java"

apply from: "../../maven_upload.gradle"

repositories {
    mavenLocal()
}

dependencies {
    compile 'com.netopyr.reduxfx:reduxfx-store:0.4.1'
    compile project(":devtool")
}